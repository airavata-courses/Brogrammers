apiVersion: v1
kind: Service
metadata:
  name: modelexecution
spec:
 type: LoadBalancer
 ports:
  - targetPort: 9010
    port: 9010
    nodePort: 30060
 selector:
  app: app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: modelexecution
spec:
  selector:
    matchLabels:
      app: app
  replicas: 2
  template:
        imagePullPolicy: Always
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: modelexecution
        image: arjunbh/model-execution
        ports:
        - containerPort: 9010
      restartPolicy: Always
