package com.usermanagement.controller;

import org.springframework.stereotype.Component;

@Component
public class UserDetailSender {
	
	/*
	 * @Autowired private Utils utils;
	 * 
	 * @Autowired private UserRepository userRepository;
	 * 
	 * @GetMapping("/all") public List<User> getAll(){ return
	 * userRepository.findAll(); }
	 * 
	 * @PutMapping("/save") public User save(User userBean){
	 * userBean.setName("Ishita"); userBean.setAddress("HC");
	 * userBean.setEmailID("ishkumar@iu.edu"); return userRepository.save(userBean);
	 * }
	 */
}
