apiVersion: v1
kind: Service
metadata:
  name: postanalysis
spec:
 type: LoadBalancer
 ports:
  - targetPort: 9020
    port: 9020
    nodePort: 30070
 selector:
  app: app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postanalysis
spec:
  selector:
    matchLabels:
      app: app
  replicas: 5
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: post-analysis
        imagePullPolicy: Always
        image: arjunbh/post-analysis
        ports:
        - containerPort: 9020
      restartPolicy: Always
