apiVersion: v1
kind: Service
metadata:
  name: data-retrieval
spec:
 type: LoadBalancer
 ports:
  - targetPort: 9002
    port: 9002
    nodePort: 30050
 selector:
  app: python
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-retrieval
spec:
  selector:
    matchLabels:
      app: python
  replicas: 1
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: data-retrieval
        imagePullPolicy: Always
        image: arjunbh/data-retrieval
        ports:
        - containerPort: 9002
